╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                      scQCFilter R包 - 安装指南                           ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

✨ 更新说明
═════════════════════════════════════════════════════════════════════════════

本版本已更新：
✓ 所有函数已使用 package::function 格式标记
✓ 避免了命名空间冲突
✓ 可以安全地与其他包并存使用

═════════════════════════════════════════════════════════════════════════════

📦 安装步骤
═════════════════════════════════════════════════════════════════════════════

方式1: 直接source（最简单）
─────────────────────────────────────────────────────────────────────────

1. 解压 scQCFilter.tar.gz
   tar -xzf scQCFilter.tar.gz

2. 在R中运行：
   source("path/to/scQCFilter/scQCFilter_main.R")

3. 立即可用：
   result <- scQCFilter(your_seurat_obj)


方式2: 作为R包安装
─────────────────────────────────────────────────────────────────────────

1. 解压
   tar -xzf scQCFilter.tar.gz

2. 在R中安装：
   devtools::install("path/to/scQCFilter")
   
   或者
   
   install.packages("path/to/scQCFilter", repos=NULL, type="source")

3. 加载：
   library(scQCFilter)

4. 使用：
   result <- scQCFilter(your_seurat_obj)

═════════════════════════════════════════════════════════════════════════════

⚠️  依赖包安装
═════════════════════════════════════════════════════════════════════════════

使用前请确保已安装以下包：

if (!require("Seurat")) install.packages("Seurat")
if (!require("ggplot2")) install.packages("ggplot2")
if (!require("dplyr")) install.packages("dplyr")
if (!require("tidyr")) install.packages("tidyr")
if (!require("ComplexHeatmap")) {
  if (!require("BiocManager")) install.packages("BiocManager")
  BiocManager::install("ComplexHeatmap")
}

═════════════════════════════════════════════════════════════════════════════

🚀 快速开始
═════════════════════════════════════════════════════════════════════════════

# 1. 加载包
source("scQCFilter_main.R")

# 2. 运行分析
result <- scQCFilter(your_seurat_obj,
                     percent.mt.max = 20,
                     groups.by = "cell_type",
                     sample.by = "orig.ident")

# 3. 查看报告
browseURL(result$report_file)

═════════════════════════════════════════════════════════════════════════════

📄 文件说明
═════════════════════════════════════════════════════════════════════════════

scQCFilter/
├── R/
│   ├── scQCFilter_main.R      # 主函数实现
│   └── scQCFilter_examples.R  # 示例代码
├── man/
│   └── (文档文件)
├── docs/
│   ├── README.md              # 项目介绍
│   ├── QUICK_REFERENCE.md     # 快速参考
│   ├── scQCFilter_usage_guide.md
│   ├── scQCFilter_development_guide.md
│   ├── scQCFilter_three_dimensions.md
│   └── PROJECT_SUMMARY.md
├── DESCRIPTION                # 包元数据
├── NAMESPACE
└── START_HERE.txt            # 开始阅读

═════════════════════════════════════════════════════════════════════════════

✅ 命名空间标记
═════════════════════════════════════════════════════════════════════════════

所有可能产生冲突的函数都已标记为：
✓ dplyr::filter, dplyr::select, dplyr::mutate 等
✓ ggplot2::ggplot, ggplot2::aes, ggplot2::geom_bar 等
✓ tidyr::expand_grid 等
✓ Seurat::CreateSeuratObject 等
✓ base::data.frame, base::list 等

这样即使加载多个包也不会产生冲突！

═════════════════════════════════════════════════════════════════════════════

❓ 常见问题
═════════════════════════════════════════════════════════════════════════════

Q: 包加载失败，提示缺少依赖？
A: 运行上面的"依赖包安装"部分的代码

Q: source()后找不到scQCFilter函数？
A: 检查路径是否正确，确保在correct目录执行

Q: 报告没有生成？
A: 检查是否有写入权限，尝试自定义report.dir

Q: 出现函数冲突错误？
A: 已使用package::function格式，不应该有冲突

═════════════════════════════════════════════════════════════════════════════

📚 文档
═════════════════════════════════════════════════════════════════════════════

- START_HERE.txt                 → 快速入门指南
- README.md                      → 项目介绍
- QUICK_REFERENCE.md             → 常用命令速查表
- scQCFilter_usage_guide.md      → 详细使用指南
- scQCFilter_development_guide.md → 开发指南
- scQCFilter_three_dimensions.md → 架构设计文档
- PROJECT_SUMMARY.md             → 项目完成总结

═════════════════════════════════════════════════════════════════════════════

🎉 祝你使用愉快！
═════════════════════════════════════════════════════════════════════════════
